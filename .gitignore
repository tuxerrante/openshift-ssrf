# Whitelist approach: ignore everything except the files we explicitly allow below.
# Ignore everything
*

# Un-ignore directories so we can allow files inside folders
!*/

# Keep the .gitignore tracked
!.gitignore

# Allow Go source files anywhere in the repo
!*.go
!**/*.go

# Allow module files
!go.mod
!go.sum

# Allow README / readme files (README, README.md, readme, readme.md, etc.)
!README
!README.*

# Allow json files inside the output directory
!output/
!output/*.json
!output/**/*.json
